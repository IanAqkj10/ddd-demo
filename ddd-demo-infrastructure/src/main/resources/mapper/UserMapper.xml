<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.ddd.infrastructure.iam.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.example.ddd.infrastructure.iam.dataobject.UserDO">
        SELECT user_id, username, password_hash, password_salt, status, role_ids, created_at, last_updated_at
        FROM t_iam_user
        WHERE username = #{username}
    </select>

</mapper>
